{
  "name": "joveworks",
  "version": "0.9.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "pnpm --filter @joveworks/editor dev",
    "dev:all": "pnpm run --parallel '/^(dev|docs:dev)$/'",
    "docs:dev": "pnpm --filter @joveworks/docs-site dev",
    "docs:build": "pnpm --filter @joveworks/docs-site build",
    "encrypt-catalogue": "node tools/encrypt-catalogue/encrypt-catalogue.mjs",
    "release": "commit-and-tag-version"
  },
  "devDependencies": {
    "commit-and-tag-version": "^13.1.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
